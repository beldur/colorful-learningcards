// @flow

// const NAME = 'auth'
// const NAVIGATE = `${NAME}/NAVIGATE`

const initialState = {
  authenticated: false,
  user: {},
}

export default function reducer(state: Object = initialState, action: Object) {
  switch (action.type) {
    default:
      return state
  }
}
